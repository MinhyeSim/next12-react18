import React, {useEffect, useState} from "react";
import UserLogin from 'react'

const UserLoginPage = () => {
    const onSubmit = e =>{
        e.preventDefault()
        const{} = from
    }
    return(<UserLogin onSubit = {onSubmit} onChange={onChange} from={form}/>)
}
export default UserLoginPage

